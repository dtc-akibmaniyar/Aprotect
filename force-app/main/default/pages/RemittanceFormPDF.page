<apex:page controller="RemittanceFormPDFController" renderAs="pdf" showHeader="false" sidebar="false" standardStylesheets="false" applyBodyTag="false" applyHtmlTag="false">
    <html>
        <head>
            <style>
                @page {
                    size: letter;
                    margin: 25mm;
                    @bottom-center {
                        content: "Page " counter(page) " of " counter(pages);
                    }
                }
                .page-break {
                    page-break-before: always;
                }
                body {
                    font-family: Arial, sans-serif;
                    margin: 0;
                    padding: 0;
                    color: #333;
                    font-size: 11px;
                }
                .page-content {
                    padding: 20px;
                }
            </style>
        </head>
        <body>
            <!-- PAGE 1: REMITTANCE FORM SUMMARY -->
            <div class="page-content">

                <!-- HEADER: Remittance Form + ID -->
                <div style="padding-bottom:8px; margin-bottom:20px;">
                    <div style="font-size:15px; font-weight:600; color:#5184B8; font-family:Inter, Arial, sans-serif;">
                        <apex:outputPanel rendered="{!isAllocationRemittance}">Allocation Remittance Form</apex:outputPanel>
                        <apex:outputPanel rendered="{!!isAllocationRemittance}">Remittance Form</apex:outputPanel>
                    </div>
                    <div style="font-size:9px; color:#333; margin-top:2px;">Remittance ID # {!remittanceForm.Name}</div>
                </div>

                <!-- DATE & STATUS ROW -->
                <div style="display:table; width:100%; margin-bottom:20px;">
                    <div style="display:table-cell; width:33%; vertical-align:top;">
                        <div style="font-size:10px; font-weight:bold; color:#333;">Remittance Date:</div>
                        <div style="font-size:10px; color:#666;">
                            <apex:outputText rendered="{!remittanceDate != ''}" value="{!remittanceDate}"/>
                            <apex:outputText rendered="{!remittanceDate == ''}" value="N/A"/>
                        </div>
                    </div>
                    <div style="display:table-cell; width:33%; vertical-align:top;">
                        <div style="font-size:10px; font-weight:bold; color:#333;">Date Generated:</div>
                        <div style="font-size:10px; color:#666;">
                            <apex:outputText rendered="{!remittanceForm.CreatedDate != null}" value="{0,date,MMM dd, yyyy}">
                                <apex:param value="{!remittanceForm.CreatedDate}"/>
                            </apex:outputText>
                        </div>
                    </div>
                    <div style="display:table-cell; width:34%; vertical-align:top; text-align:right;">
                        <div style="font-family:Inter, Arial, sans-serif; font-size:10px; font-weight:600; color:#000000;">Status:</div>
                        <apex:outputPanel rendered="{!remittanceForm.Status__c == 'Completed'}">
                            <div style="font-family:Inter, Arial, sans-serif; font-size:22px; font-weight:bold; color:#5cb85c;">{!remittanceForm.Status__c}</div>
                        </apex:outputPanel>
                        <apex:outputPanel rendered="{!remittanceForm.Status__c != 'Completed'}">
                            <div style="font-family:Inter, Arial, sans-serif; font-size:22px; font-weight:bold; color:#d9534f;">Due</div>
                        </apex:outputPanel>
                    </div>
                </div>

                <!-- BILL TO & TOTAL REMITTANCE BALANCE -->
                <div style="display:table; width:100%; margin-bottom:20px;">
                    <div style="display:table-cell; width:50%; vertical-align:top;">
                        <div style="font-size:12px; font-weight:bold; color:#333; margin-bottom:4px;">
                            <apex:outputPanel rendered="{!remittanceForm.Dealer_Name__r != null}">{!remittanceForm.Dealer_Name__r.Name}</apex:outputPanel>
                        </div>
                        <div style="font-size:11px; color:#333; line-height:1.6;">
                            <apex:outputText escape="false" value="{!remittanceForm.Bill_To__c}"/>
                        </div>
                    </div>
                    <div style="display:table-cell; width:50%; vertical-align:middle; text-align:right;">
                        <div style="font-size:11px; font-weight:bold; color:#333;">Total Remittance Balance</div>
                        <div style="font-size:24px; font-weight:bold; color:#333;">
                            <apex:outputText value="${0,number,###,##0.00}"><apex:param value="{!grandTotal}"/></apex:outputText>
                        </div>
                    </div>
                </div>

                <!-- INVOICE SUMMARY TABLE (6 columns) -->
                <table style="width:100%; border-collapse:collapse; margin-bottom:10px;">
                    <tr style="background:#4a7fb5;">
                        <td style="padding:6px 8px; font-size:8px; font-weight:bold; color:#fff; border:1px solid #3a6a9a;">App ID</td>
                        <td style="padding:6px 8px; font-size:8px; font-weight:bold; color:#fff; border:1px solid #3a6a9a;">Customer Name</td>
                        <td style="padding:6px 8px; font-size:8px; font-weight:bold; color:#fff; border:1px solid #3a6a9a;">Vehicle</td>
                        <td style="padding:6px 8px; font-size:8px; font-weight:bold; color:#fff; border:1px solid #3a6a9a;">VIN #</td>
                        <td style="padding:6px 8px; font-size:8px; font-weight:bold; color:#fff; border:1px solid #3a6a9a;">Invoice #</td>
                        <td style="padding:6px 8px; font-size:8px; font-weight:bold; color:#fff; border:1px solid #3a6a9a; text-align:right;">Balance</td>
                    </tr>
                    <apex:repeat var="invWithItems" value="{!invoicesWithItems}">
                        <tr>
                            <td style="padding:5px 8px; font-size:8px; color:#333; border:1px solid #ddd;">{!invWithItems.invoice.Application_Id__c}</td>
                            <td style="padding:5px 8px; font-size:8px; color:#333; border:1px solid #ddd;">{!invWithItems.invoice.Customer_Name__c}</td>
                            <td style="padding:5px 8px; font-size:8px; color:#333; border:1px solid #ddd;">{!invWithItems.invoice.Vehicle_Year__c}/{!invWithItems.invoice.Vehicle_Make__c}/{!invWithItems.invoice.Vehicle_Model__c}/{!invWithItems.invoice.Vehicle_Trim__c}</td>
                            <td style="padding:5px 8px; font-size:8px; color:#333; border:1px solid #ddd;">{!invWithItems.invoice.Vin__c}</td>
                            <td style="padding:5px 8px; font-size:8px; color:#333; border:1px solid #ddd;">{!invWithItems.invoiceName}</td>
                            <td style="padding:5px 8px; font-size:8px; color:#333; border:1px solid #ddd; text-align:right;">
                                <apex:outputText value="${0,number,###,##0.00}"><apex:param value="{!invWithItems.invoiceGrandTotal}"/></apex:outputText>
                            </td>
                        </tr>
                    </apex:repeat>
                </table>

                <!-- Items Count -->
                <div style="font-size:9px; font-weight:bold; color:#333; margin-top:4px; margin-bottom:10px;">
                    <strong>Items:</strong> {!invoicesWithItems.size}
                </div>

                <!-- BOTTOM TOTALS -->
                <table style="width:50%; border-collapse:collapse; margin-left:auto;">
                    <tr>
                        <td style="padding:6px 10px; font-size:9px; font-weight:bold; color:#333; text-align:right; border-bottom:1px solid #ddd;">Total Remittance Due:</td>
                        <td style="padding:6px 10px; font-size:9px; color:#333; text-align:right; border-bottom:1px solid #ddd;">
                            <apex:outputText value="${0,number,###,##0.00}"><apex:param value="{!grandTotal}"/></apex:outputText>
                        </td>
                    </tr>
                    <apex:outputPanel layout="none" rendered="{!remittanceForm.Discount_Amount__c != null}">
                        <tr>
                            <td style="padding:6px 10px; font-size:9px; font-weight:bold; font-style:italic; color:#333; text-align:right; border-bottom:1px solid #ddd;">(-) Credits Applied:</td>
                            <td style="padding:6px 10px; font-size:9px; color:#333; text-align:right; border-bottom:1px solid #ddd;">
                                (<apex:outputText value="${0,number,###,##0.00}"><apex:param value="{!remittanceForm.Discount_Amount__c}"/></apex:outputText>)
                            </td>
                        </tr>
                    </apex:outputPanel>
                    <tr style="border-top:2px solid #333;">
                        <td style="padding:8px 10px; font-size:10px; font-weight:bold; color:#333; text-align:right;">Balance Due:</td>
                        <td style="padding:8px 10px; font-size:11px; font-weight:bold; color:#333; text-align:right;">
                            <apex:outputText value="${0,number,###,##0.00}">
                                <apex:param value="{!IF(remittanceForm.Balance__c != null, remittanceForm.Balance__c, IF(remittanceForm.Remittance_Form_Total_After_Discount__c != null, remittanceForm.Remittance_Form_Total_After_Discount__c, grandTotal))}"/>
                            </apex:outputText>
                        </td>
                    </tr>
                </table>

            </div>

            <!-- PAGES 2+: INDIVIDUAL INVOICES (ONE PER PAGE) -->
            <apex:outputPanel rendered="{!invoicesWithItems.size > 0}">
                <apex:repeat var="invWithItems" value="{!invoicesWithItems}" rendered="{!invoicesWithItems.size > 0}">
                    <div class="page-break page-content">

                        <!-- INVOICE HEADER -->
                        <div style="border-bottom:2px solid #5184B8; padding-bottom:8px; margin-bottom:20px;">
                            <div style="font-size:15px; font-weight:600; color:#5184B8; font-family:Inter, Arial, sans-serif;">INVOICE</div>
                            <div style="font-size:9px; color:#333; margin-top:2px;">Invoice ID # {!invWithItems.invoiceName}</div>
                        </div>

                        <!-- DATE ROW -->
                        <div style="display:table; width:100%; margin-bottom:20px;">
                            <div style="display:table-cell; width:50%; vertical-align:top;">
                                <div style="font-family:Inter, Arial, sans-serif; font-size:10px; font-weight:600; color:#000;">Date:</div>
                                <div style="font-size:10px; color:#666;">
                                    <apex:outputText rendered="{!invWithItems.invoice.CreatedDate != null}" value="{0,date,MMM dd, yyyy}">
                                        <apex:param value="{!invWithItems.invoice.CreatedDate}"/>
                                    </apex:outputText>
                                </div>
                            </div>
                        </div>

                        <!-- TWO-COLUMN LAYOUT: Customer/Vehicle (40%) | Package Breakdown (60%) -->
                        <div style="display:table; width:100%; margin-bottom:16px;">

                            <!-- LEFT COLUMN (40%): Customer + Vehicle Info -->
                            <div style="display:table-cell; width:40%; vertical-align:top; padding-right:12px;">

                                <!-- Customer Information -->
                                <div style="margin-bottom:12px;">
                                    <div style="background:#5184B8; color:#fff; padding:6px 8px; font-size:8px; font-weight:bold; text-transform:uppercase; letter-spacing:0.5px;">Customer Information</div>
                                    <div style="border:1px solid #ddd; border-top:none; padding:8px; font-size:9px; color:#333; line-height:1.8;">
                                        <div><strong>Name:</strong> {!invWithItems.application.Customer_First_Name__c} {!invWithItems.application.Customer_Last_Name__c}</div>
                                        <apex:outputPanel rendered="{!invWithItems.application.Customer_Address__c != null}">
                                            <div><strong>Address:</strong> {!invWithItems.application.Customer_Address__c}</div>
                                        </apex:outputPanel>
                                        <apex:outputPanel rendered="{!invWithItems.application.Customer_Phone__c != null}">
                                            <div><strong>Phone:</strong> {!invWithItems.application.Customer_Phone__c}</div>
                                        </apex:outputPanel>
                                    </div>
                                </div>

                                <!-- Vehicle Information -->
                                <div>
                                    <div style="background:#5184B8; color:#fff; padding:6px 8px; font-size:8px; font-weight:bold; text-transform:uppercase; letter-spacing:0.5px;">Vehicle Information</div>
                                    <div style="border:1px solid #ddd; border-top:none; padding:8px; font-size:9px; color:#333; line-height:1.8;">
                                        <div><strong>Vehicle:</strong> {!invWithItems.application.Vehicle_Year__c}/{!invWithItems.application.Vehicle_Make__c}/{!invWithItems.application.Vehicle_Model__c}</div>
                                        <div><strong>VIN:</strong> {!invWithItems.application.Vehicle_VIN__c}</div>
                                        <div><strong>Odometer:</strong> {!invWithItems.application.Odometer__c} {!invWithItems.application.Odometer_Unit__c}</div>
                                        <apex:outputPanel rendered="{!invWithItems.application.Vehicle__r != null && invWithItems.application.Vehicle__r.Delivery_Date__c != null}">
                                            <div><strong>Vehicle Delivery Date:</strong> <apex:outputText value="{0,date,MMM dd, yyyy}"><apex:param value="{!invWithItems.application.Vehicle__r.Delivery_Date__c}"/></apex:outputText></div>
                                        </apex:outputPanel>
                                    </div>
                                </div>

                            </div>

                            <!-- RIGHT COLUMN (60%): Package Breakdown -->
                            <div style="display:table-cell; width:60%; vertical-align:top;">
                                <table style="width:100%; border-collapse:collapse;">
                                    <tr style="background:#5184B8;">
                                        <td style="padding:6px 6px; font-size:7px; font-weight:bold; color:#fff; border:1px solid #3a6a9a;">Package Name</td>
                                        <td style="padding:6px 6px; font-size:7px; font-weight:bold; color:#fff; border:1px solid #3a6a9a;">Type/Tier</td>
                                        <td style="padding:6px 6px; font-size:7px; font-weight:bold; color:#fff; border:1px solid #3a6a9a;">Duration</td>
                                        <td style="padding:6px 6px; font-size:7px; font-weight:bold; color:#fff; border:1px solid #3a6a9a; text-align:right;">Dealer Price</td>
                                        <td style="padding:6px 6px; font-size:7px; font-weight:bold; color:#fff; border:1px solid #3a6a9a; text-align:right;">Tax</td>
                                        <td style="padding:6px 6px; font-size:7px; font-weight:bold; color:#fff; border:1px solid #3a6a9a; text-align:right;">Subtotal</td>
                                    </tr>
                                    <apex:repeat var="pkgWithOpts" value="{!invWithItems.packages}">
                                        <tr>
                                            <td style="padding:5px 6px; font-size:8px; color:#333; border:1px solid #ddd;">{!pkgWithOpts.displayName}</td>
                                            <td style="padding:5px 6px; font-size:8px; color:#333; border:1px solid #ddd;">{!pkgWithOpts.applicationPackage.Package__r.Name}</td>
                                            <td style="padding:5px 6px; font-size:8px; color:#333; border:1px solid #ddd;">{!pkgWithOpts.duration}</td>
                                            <td style="padding:5px 6px; font-size:8px; color:#333; border:1px solid #ddd; text-align:right;">
                                                <apex:outputText value="${0,number,###,##0.00}"><apex:param value="{!pkgWithOpts.dealerPrice}"/></apex:outputText>
                                            </td>
                                            <td style="padding:5px 6px; font-size:8px; color:#333; border:1px solid #ddd; text-align:right;">
                                                <apex:outputText value="${0,number,###,##0.00}"><apex:param value="{!pkgWithOpts.taxAmount}"/></apex:outputText>
                                            </td>
                                            <td style="padding:5px 6px; font-size:8px; color:#333; border:1px solid #ddd; text-align:right;">
                                                <apex:outputText value="${0,number,###,##0.00}"><apex:param value="{!pkgWithOpts.subtotal}"/></apex:outputText>
                                            </td>
                                        </tr>
                                    </apex:repeat>
                                </table>
                            </div>

                        </div>

                        <!-- BOTTOM: QTY & TOTAL -->
                        <div style="display:table; width:100%; margin-top:10px;">
                            <div style="display:table-cell; width:50%; vertical-align:bottom;">
                                <div style="font-size:9px; font-weight:bold; color:#333;">
                                    <strong>Qty:</strong> {!invWithItems.packages.size}
                                </div>
                            </div>
                            <div style="display:table-cell; width:50%; vertical-align:top;">
                                <table style="width:100%; border-collapse:collapse; margin-left:auto;">
                                    <tr>
                                        <td style="padding:6px 10px; font-size:10px; font-weight:bold; color:#333; text-align:right; border:1px solid #ddd;">Total Amount:</td>
                                        <td style="padding:6px 10px; font-size:10px; font-weight:bold; color:#333; text-align:right; border:1px solid #ddd;">
                                            <apex:outputText value="${0,number,###,##0.00}"><apex:param value="{!invWithItems.invoiceGrandTotal}"/></apex:outputText>
                                        </td>
                                    </tr>
                                </table>
                            </div>
                        </div>

                    </div>
                </apex:repeat>
            </apex:outputPanel>
        </body>
    </html>
</apex:page>